@* @{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
 *@

@{
    ViewData["Title"] = "Ana Sayfa";
}

<h2>@ViewData["Title"]</h2>
<p>Hoşgeldiniz, lütfen giriş yönteminizi seçin:</p>

<a class="btn btn-primary" href="@Url.Action("Login", "User", new { userType = "Administrator" })">İdareci Girişi</a>
<a class="btn btn-primary" href="@Url.Action("Login", "User", new { userType = "Faculty" })">Öğretim Elemanı Girişi</a>
<a class="btn btn-secondary" href="@Url.Action("AnonymousView", "Home")">Anonim Giriş</a>
